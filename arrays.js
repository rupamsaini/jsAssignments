
//Marvels

const heroes = ['Iron Man', 'Superman', 'America']

console.log(heroes[0])
console.log(heroes[heroes.length - 1]) //gets the last element

console.log(`We have ${heroes.length} heroes`) //''(tilde) used to execute js code in ${}
console.log('We have '+ heroes.length +' heroes')